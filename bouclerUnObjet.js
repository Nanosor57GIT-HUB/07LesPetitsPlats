


 //Itérer ou boucler un objet js

    //Récupère chaque tableau avec éléments(in)/Récupère chaque éléments(of)
    // for (let key in temp) {
    //   console.log(key, ":", temp[key]);
    //   for(let tmp of temp[key])
    //   console.log(tmp);
    // }

    //Récupère chaque tableau avec éléments
    // for (let keyArrays of Object.getOwnPropertyNames(temp)) {
    //   const arraysKey= temp[keyArrays];
    //   console.log(keyArrays, ":", arraysKey);
    // }

    //Récupère chaque tableau avec éléments
    // for (let [key, element] of Object.entries(temp)) {
    //   console.log(key, ":", element);
    // }

    //Récupère chaque tableau avec éléments
    // for (let elem of Object.keys(temp)) {
    //   console.log(elem, ":", temp[elem]);
    // }